# 🚀 راهنمای نصب و راه‌اندازی سیستم احراز هویت

این راهنما به صورت مرحله‌به‌مرحله توضیح می‌دهد که چطور سیستم احراز هویت را راه‌اندازی کنی.

---

## ✅ مرحله 1: گرفتن کلید از Google

### چرا؟
برای اینکه Google به ما اجازه بده کاربران را با حساب Google وارد کنیم.

### چطور؟

1. **برو به Google Cloud Console:**
   - آدرس: https://console.cloud.google.com/
   - اگر حساب نداری، یک حساب بساز (رایگان است)

2. **ساخت پروژه جدید:**
   - روی "Select a project" کلیک کن
   - "New Project" را بزن
   - نام پروژه: `Active Legend` (یا هر نامی که دوست داری)
   - "Create" را بزن

3. **فعال کردن API:**
   - از منوی سمت چپ: **APIs & Services** → **Library**
   - در جستجو بنویس: `Google Identity`
   - روی "Google Identity Services API" کلیک کن
   - "Enable" را بزن

4. **ساخت OAuth Credentials:**
   - از منوی سمت چپ: **APIs & Services** → **Credentials**
   - روی "Create Credentials" کلیک کن
   - "OAuth 2.0 Client ID" را انتخاب کن
   
   - اگر اولین بار است، باید OAuth consent screen را تنظیم کنی:
     - User Type: **External** را انتخاب کن
     - App name: `Active Legend`
     - User support email: ایمیل خودت
     - Developer contact: ایمیل خودت
     - "Save and Continue" را بزن
     - Scopes: هیچی اضافه نکن، فقط "Save and Continue"
     - Test users: هیچی اضافه نکن، "Save and Continue"
     - Summary: "Back to Dashboard" را بزن

   - حالا دوباره: **Create Credentials** → **OAuth 2.0 Client ID**
   - Application type: **Web application**
   - Name: `Active Legend Auth`
   - **Authorized redirect URIs**: این را اضافه کن:
     ```
     https://activelegend.ir/api/auth/callback/google
     ```
   - "Create" را بزن

5. **کپی کردن کلیدها:**
   - یک پنجره باز می‌شود با دو چیز:
     - **Client ID** (مثل: `123456789-abc...`)
     - **Client Secret** (مثل: `GOCSPX-xyz...`)
   - هر دو را کپی کن و در یک فایل متنی ذخیره کن (بعداً نیاز داری)

---

## ✅ مرحله 2: نصب Wrangler

### چرا؟
Wrangler ابزار Cloudflare است که با آن می‌توانیم کد را روی Cloudflare آپلود کنیم.

### چطور؟

1. **باز کردن ترمینال:**
   - در VS Code: `Ctrl + ~` (یا Terminal → New Terminal)
   - یا PowerShell یا CMD را باز کن

2. **نصب Wrangler:**
   ```bash
   npm install -g wrangler
   ```
   
   صبر کن تا نصب شود (چند دقیقه طول می‌کشد)

3. **بررسی نصب:**
   ```bash
   wrangler --version
   ```
   
   باید یک شماره نسخه نشان بدهد (مثلاً: `3.0.0`)

---

## ✅ مرحله 3: لاگین به Cloudflare

### چرا؟
برای اینکه Cloudflare بداند این پروژه مال توست.

### چطور؟

1. **ساخت حساب Cloudflare (اگر نداری):**
   - برو به: https://dash.cloudflare.com/sign-up
   - یک حساب رایگان بساز

2. **لاگین در ترمینال:**
   ```bash
   wrangler login
   ```
   
   - مرورگر باز می‌شود
   - با حساب Cloudflare وارد شو
   - "Allow" را بزن

---

## ✅ مرحله 4: ساخت دیتابیس

### چرا؟
برای ذخیره اطلاعات کاربران (ایمیل، نام، عکس).

### چطور؟

1. **ساخت دیتابیس:**
   ```bash
   wrangler d1 create activelegend-db
   ```

2. **کپی کردن Database ID:**
   - یک خروجی می‌دهد شبیه این:
     ```
     [[d1_databases]]
     binding = "DB"
     database_name = "activelegend-db"
     database_id = "abc123-def456-ghi789"
     ```
   - **database_id** را کپی کن (بعداً نیاز داری)

3. **اجرای Migration (ساخت جدول):**
   ```bash
   wrangler d1 execute activelegend-db --file=workers/schema.sql
   ```
   
   باید پیام موفقیت بدهد.

---

## ✅ مرحله 5: تنظیم Secrets

### چرا؟
برای ذخیره کلیدهای Google و JWT به صورت امن.

### چطور؟

1. **تنظیم Google Client ID:**
   ```bash
   wrangler secret put GOOGLE_CLIENT_ID
   ```
   - وقتی ازت خواست، Client ID که از Google کپی کردی را paste کن
   - Enter را بزن

2. **تنظیم Google Client Secret:**
   ```bash
   wrangler secret put GOOGLE_CLIENT_SECRET
   ```
   - Client Secret را paste کن
   - Enter را بزن

3. **تنظیم JWT Secret:**
   ```bash
   wrangler secret put JWT_SECRET
   ```
   - یک رمز قوی وارد کن (حداقل 32 کاراکتر)
   - می‌توانی از این استفاده کنی:
     ```
     active-legend-super-secret-jwt-key-2024-production
     ```
   - Enter را بزن

---

## ✅ مرحله 6: به‌روزرسانی wrangler.toml

### چرا؟
برای اینکه Worker بداند از کدام دیتابیس استفاده کند.

### چطور؟

1. **باز کردن فایل:**
   - فایل `wrangler.toml` را در ریشه پروژه باز کن

2. **جایگزینی Database ID:**
   - خط 11 را پیدا کن:
     ```toml
     database_id = "YOUR_D1_DATABASE_ID"
     ```
   - `YOUR_D1_DATABASE_ID` را با database_id که از مرحله 4 کپی کردی جایگزین کن
   - مثلاً:
     ```toml
     database_id = "abc123-def456-ghi789"
     ```

3. **ذخیره فایل**

---

## ✅ مرحله 7: Deploy کردن Worker

### چرا؟
برای اینکه کد روی Cloudflare اجرا شود.

### چطور؟

1. **بررسی فایل Worker:**
   - مطمئن شو فایل `workers/src/auth.ts` وجود دارد

2. **Deploy:**
   ```bash
   wrangler deploy --env production
   ```
   
   - چند دقیقه طول می‌کشد
   - در آخر یک URL می‌دهد (مثلاً: `activelegend-auth.workers.dev`)

---

## ✅ مرحله 8: تنظیم Routes در Cloudflare

### چرا؟
برای اینکه درخواست‌های `/api/auth/*` به Worker ما برود.

### چطور؟

1. **رفتن به Cloudflare Dashboard:**
   - برو به: https://dash.cloudflare.com/
   - روی دامنه `activelegend.ir` کلیک کن

2. **تنظیم Workers Routes:**
   - از منوی سمت چپ: **Workers & Pages**
   - روی "activelegend-auth" Worker کلیک کن
   - تب "Triggers" را باز کن
   - در بخش "Routes" روی "Add route" کلیک کن
   - Route: `activelegend.ir/api/auth/*`
   - Zone: `activelegend.ir`
   - "Save" را بزن

---

## ✅ مرحله 9: تنظیم Environment Variable در Frontend

### چرا؟
برای اینکه Frontend بداند API کجاست.

### چطور؟

1. **ساخت فایل .env:**
   - در ریشه پروژه یک فایل به نام `.env` بساز
   - محتوای آن:
     ```
     VITE_API_URL=https://activelegend.ir
     ```

2. **ذخیره فایل**

---

## ✅ مرحله 10: تست کردن

### چطور؟

1. **Build کردن Frontend:**
   ```bash
   npm run build
   ```

2. **Deploy کردن Frontend:**
   - اگر از GitHub Pages استفاده می‌کنی:
     ```bash
     npm run deploy
     ```

3. **تست Login:**
   - برو به سایت: https://activelegend.ir
   - روی "ورود" کلیک کن
   - "ورود با گوگل" را بزن
   - باید به Google هدایت شوی
   - بعد از تایید، باید به سایت برگردی و وارد شده باشی

---

## ❌ اگر مشکلی پیش آمد:

### مشکل: wrangler command not found
**راه حل:** دوباره نصب کن:
```bash
npm install -g wrangler
```

### مشکل: Error در deploy
**راه حل:** 
- مطمئن شو همه secrets را تنظیم کردی
- مطمئن شو database_id درست است
- لاگ‌ها را ببین: `wrangler tail`

### مشکل: Login کار نمی‌کند
**راه حل:**
- مطمئن شو redirect URI در Google درست است
- مطمئن شو Route در Cloudflare تنظیم شده
- لاگ‌ها را ببین: `wrangler tail`

---

## 📞 کمک بیشتر:

اگر مشکلی داشتی:
1. لاگ‌های Worker را ببین: `wrangler tail`
2. Console مرورگر را چک کن (F12)
3. Network tab را چک کن (F12 → Network)

---

**موفق باشی! 🎉**
